#include "../main.h"

/**
 * main - check the code for ALX School students
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
