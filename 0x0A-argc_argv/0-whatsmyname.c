#include <stdlib.h>
#include <stdio.h>

/**
 * main- a program that prints its name
 *
 * @i: used to access argv array elements
 * @argv: program array input
 *
 * Return: Always 0 (Success)
*/

int main(int i, char *argv[])
{
	i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
